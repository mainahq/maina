---
title: MCP Server
description: Add Maina's verification, context, and review tools to any MCP-compatible IDE.
---

import { Code, Tabs, TabItem } from '@astrojs/starlight/components';
import { facts } from '../../data/facts';

Maina includes an MCP (Model Context Protocol) server that exposes its engines to any MCP-compatible IDE -- Claude Code, Cursor, Windsurf, and others.

## Setup

The fastest path is one command — it auto-detects every supported client on your machine and writes the maina entry into each global config:

```bash
maina mcp add
```

To remove maina from every client just as quickly:

```bash
maina mcp remove
```

Or scope to specific clients:

```bash
maina mcp add --client claude,cursor,windsurf
maina mcp add --scope project          # write the repo's files (.mcp.json, .cursor/mcp.json) instead of global
maina mcp add --dry-run                # preview the writes without touching disk
maina mcp list                         # show install status per client
```

Supported clients: **Claude Code, Cline, Codex, Continue, Copilot, Cursor, Gemini CLI, OpenHands, Roo Code, Windsurf, Zed AI** (any MCP client works — those eleven are auto-detected). Each entry goes into the file the client actually reads — for Claude Code that is `~/.claude.json` (global) or `.mcp.json` (project), never `settings.json`. The command preserves all other MCP servers and unrelated keys (hooks, permissions, comments in Codex's TOML) via an atomic merge, backs the original up once under `.maina/backups/`, and is safe to run repeatedly. `maina mcp remove` restores the pre-maina file exactly when nothing else changed since, deletes a file maina created, and otherwise removes only the `maina` entry.

The launcher is auto-detected per machine, in this priority order:

1. **The installed CLI itself** when you run `maina mcp add` / `maina setup` from a global install (`bun install -g @mainahq/cli` or `npm install -g @mainahq/cli`): the entry is the runtime and the CLI entry file, both by absolute path (`/Users/.../.bun/bin/bun /Users/.../@mainahq/cli/dist/index.js --mcp`). Fastest by far — no package manager and no shebang in the spawn path, so it starts even when the client's PATH has no Bun or Node. **Recommended.**
2. **Direct `maina` binary** when one is on PATH but the command ran from a throwaway copy (e.g. `bunx @mainahq/cli setup`).
3. **Resolved `bunx @mainahq/cli@<version> --mcp`** when [Bun](https://bun.sh) is available.
4. **Resolved `npx @mainahq/cli@<version> --mcp`** as the universal fallback.
5. **Bare `npx @mainahq/cli@<version> --mcp`** as the last-resort fallback when nothing else resolves on the install machine — entry stays syntactically valid; user can edit after installing Node or Bun.

Tiers 1-4 are written using **absolute** paths (`/opt/homebrew/bin/bunx`, `/Users/.../.bun/bin/maina`, etc.) so MCP clients with stripped-PATH spawn contexts (Cursor, Zed, Claude Code desktop) can find them. Tier 5 is the only case where the bare command name is written. `<version>` is the version of the CLI that wrote the entry. Manual examples below use `npx` for portability.

The setup wizard (`maina setup`) also writes the project-scoped configs (`.mcp.json`, `.cursor/mcp.json`) for you using a **keyed JSON merge** — every existing `mcpServers.*` entry is preserved byte-for-byte and only the `maina` key is written — and registers maina in the global config of installed clients that have no project file (Codex, Windsurf, Zed, …). Safe to re-run; safe against hand-edited configs. The `curl … | bash` installer only installs the package and runs `maina setup`; it writes no config itself. Most users won't need `maina mcp add` unless they want maina available in every project from any client.

Or configure manually per tool:

<Tabs>
  <TabItem label="Claude Code">
    `.mcp.json` in the repo (merged by `maina setup`) or `~/.claude.json` (`maina mcp add`). Claude Code does not read MCP servers from `settings.json`:
    ```json
    {"mcpServers":{"maina":{"command":"npx","args":["@mainahq/cli","--mcp"]}}}
    ```
  </TabItem>
  <TabItem label="Cursor">
    `.cursor/mcp.json` or `.mcp.json`:
    ```json
    {"mcpServers":{"maina":{"command":"npx","args":["@mainahq/cli","--mcp"]}}}
    ```
  </TabItem>
  <TabItem label="Windsurf">
    `~/.codeium/windsurf/mcp_config.json`:
    ```json
    {"mcpServers":{"maina":{"command":"npx","args":["@mainahq/cli","--mcp"]}}}
    ```
  </TabItem>
  <TabItem label="Cline">
    `~/Library/Application Support/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json` (mac; `.config/Code/...` on Linux, `%APPDATA%/Code/...` on Windows):
    ```json
    {"mcpServers":{"maina":{"command":"npx","args":["@mainahq/cli","--mcp"]}}}
    ```
  </TabItem>
  <TabItem label="Codex CLI">
    `~/.codex/config.toml`:
    ```toml
    [mcp_servers.maina]
    command = "npx"
    args = ["@mainahq/cli", "--mcp"]
    ```
  </TabItem>
  <TabItem label="Continue.dev">
    `~/.continue/config.json` under `experimental.modelContextProtocolServers`:
    ```json
    {"experimental":{"modelContextProtocolServers":[{"name":"maina","transport":{"type":"stdio","command":"npx","args":["@mainahq/cli","--mcp"]}}]}}
    ```
  </TabItem>
  <TabItem label="Gemini CLI">
    `~/.gemini/settings.json`:
    ```json
    {"mcpServers":{"maina":{"command":"npx","args":["@mainahq/cli","--mcp"]}}}
    ```
  </TabItem>
  <TabItem label="Zed">
    `~/.config/zed/settings.json` under `context_servers`:
    ```json
    {"context_servers":{"maina":{"source":"custom","command":{"path":"npx","args":["@mainahq/cli","--mcp"]}}}}
    ```
  </TabItem>
  <TabItem label="Other (Roo, Amazon Q)">
    Not yet handled by `maina mcp add`. Configure manually under each tool's `mcpServers` block:
    ```json
    {"mcpServers":{"maina":{"command":"npx","args":["@mainahq/cli","--mcp"]}}}
    ```
    (`.roo/mcp.json` for Roo Code; `.amazonq/mcp.json` for Amazon Q.)
  </TabItem>
</Tabs>

No plugins, no extensions, no separate server process.

## Tools

The server registers a small default set at handshake, kept to eight tools or fewer so the capability payload every session streams to the host stays small:

| Tool | What it answers | Inputs |
|------|-----------------|--------|
| `verify` | Runs the verification pipeline. Returns pass/fail, findings on changed lines, and each tool's status (`passed`, `failed`, `skipped`) with the reason a tool was skipped. | `files` (omit for staged files), `base` |
| `decide` | Answers typed questions for a decision type (`finding.real`, `diff.needs_review`, ...) with the repo's policy. Each answer carries a probability distribution and its backend. | `type`, `questions`, `state` |
| `impact` | What changing files or symbols can affect: transitive callers, dependent files, covering tests and a blast score. | `files`, `symbols`, `depth` |
| `context` | Source snippets for files or a free-text query plus their call-graph neighbourhood, within a token budget, with the tokens saved. | `files`, `query`, `budgetTokens` |
| `review_triage` | Two-stage review (spec compliance, then code quality) of a diff, triaged into blocking, advisory and info findings. | `diff` or `files` + `base`, `planContent` |
| `spec_check` | Spec/plan/tasks consistency for feature directories. | `paths` |
| `receipt` | Verifies receipt JSON files against the v1 schema and their canonical hash. | `paths` |
| `status` | Server version, enabled tools, and whether the code graph, wiki and policy are ready. | — |

Every tool also takes an optional `root`: the absolute path of the repository to act on. Without it the server uses the root it resolves for itself, normally the repository it was started in (the standalone runtime also honours the project dir the host reports). Relative `files` and `paths` resolve against the root, and absolute ones must sit inside it.

Every tool answers with a text summary for the model plus structured content in a `{ data, error, meta }` envelope, described by the tool's output schema:

<Code lang="json" code={`{
  "data": { "passed": true, "tools": [{ "tool": "semgrep", "status": "skipped", "findings": 0, "durationMs": 0, "reason": "semgrep is not installed" }] },
  "error": null,
  "meta": { "tool": "verify", "root": "/repo", "version": "${facts.version}", "durationMs": 812 }
}`} />

A failed call sets `isError`, `data: null` and `error: { kind, message }`, where `kind` is `no_root`, `invalid_input`, `not_found` or `failed`.

### Allow-list

Narrow the tool set, or add the DeepWiki-compatible tools, with the `--tools` flag or the `MAINA_MCP_TOOLS` env var (the flag wins). Names are comma-separated; `default` stands for the default set:

```bash
maina --mcp --tools verify,context,status        # only these three
MAINA_MCP_TOOLS=default,ask_question maina --mcp  # the default set plus ask_question
```

Tools outside the list are never registered. Unknown names are skipped with a warning on stderr.

**DeepWiki compat** — three tools keep wire-compatibility with DeepWiki clients. They are off by default; enable them through the allow-list:

| DeepWiki-compat | Description |
|------|------------|
| `ask_question` | Ask the wiki an open-ended question; answers with source articles. |
| `read_wiki_structure` | The wiki's article index: paths, types, titles. |
| `read_wiki_contents` | The markdown of one wiki article, by its path inside the wiki. |

## How It Works

```
IDE (Claude Code, Cursor, etc.)
  |
  |  MCP protocol (JSON-RPC over stdio)
  |
  v
maina --mcp
  |
  +-- verify ---------> Verify Engine (full pipeline, per-tool status)
  +-- decide ---------> decide (policy + backends)
  +-- impact ---------> Code graph (callers, dependents, tests)
  +-- context --------> Code graph (minimal context within a budget)
  +-- review_triage --> Two-stage review
  +-- spec_check -----> Feature analyzer (spec/plan/tasks)
  +-- receipt --------> Receipt verifier (schema + canonical hash)
  +-- status ---------> Runtime and repository health
```

The MCP server is a thin surface over the same capabilities the CLI uses: each tool resolves its root, calls one capability with explicit inputs, and formats the answer.
