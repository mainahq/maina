---
title: "Commands reference"
description: "Every maina command and its options, generated from the CLI."
---

{/* Generated by scripts/docs-manifest.ts. Do not edit: run `bun run docs:generate`. */}

Every command `maina --help` lists, with its options, read from the CLI's command definitions. Run `maina <command> --help` for the same text in your terminal.

## `maina brainstorm`

Explore an idea and create a structured ticket

| Option | Description | Default |
|---|---|---|
| `-t, --title <title>` | Feature title (skips first question) |  |
| `--no-interactive` | Generate ticket without prompts |  |

## `maina ticket`

Create a GitHub Issue with module tagging

| Option | Description | Default |
|---|---|---|
| `-t, --title <title>` | Issue title |  |
| `-b, --body <body>` | Issue body |  |
| `-l, --label <label...>` | Additional labels |  |
| `-r, --repo <repo>` | Target repo (alias: maina-cloud, workkit, or owner/name) |  |
| `--strict-labels` | Abort if any label doesn't exist on the repo (default: warn and skip) |  |

## `maina plan <name>`

Create feature branch with structured plan

| Option | Description | Default |
|---|---|---|
| `--no-verify` | Skip plan verification checklist |  |
| `--no-interactive` | Skip interactive design prompts |  |

## `maina design`

Create an Architecture Decision Record

| Option | Description | Default |
|---|---|---|
| `-t, --title <title>` | ADR title |  |
| `--list` | List existing ADRs |  |
| `--no-interactive` | Skip approach proposals phase |  |
| `--auto` | Skip all interactive prompts (alias for --no-interactive) |  |
| `--hld` | Generate HLD/LLD from feature spec using AI |  |

## `maina spec`

Generate TDD test stubs from plan

| Option | Description | Default |
|---|---|---|
| `--feature-dir <dir>` | Feature directory path |  |
| `-o, --output <path>` | Output file path |  |
| `--no-red-green` | Skip red-green enforcement check |  |
| `--no-interactive` | Skip clarifying questions phase |  |
| `--auto` | Skip all interactive prompts (alias for --no-interactive) |  |

## `maina verify`

Run full verification pipeline

| Option | Description | Default |
|---|---|---|
| `--all` | Scan all files, not just changed |  |
| `--staged` | Check staged changes only (default: the whole working tree vs the base) |  |
| `--fix` | Show AI fix suggestions |  |
| `--json` | Output JSON for CI |  |
| `--base <ref>` | Base branch for diff (default: $MAINA_BASE, origin/HEAD, master, main) |  |
| `--deep` | Force the standard-tier AI semantic review (the triage already runs it on large or sensitive diffs) |  |
| `--tests` | Also run the tests the code graph says the change can break (its blast radius) |  |
| `--visual` | Run visual regression checks |  |
| `--cloud` | Run verification on maina cloud |  |

## `maina verify-receipt <path>`

Verify a Maina receipt JSON file against the v1 schema + canonical hash

| Option | Description | Default |
|---|---|---|
| `--json` | emit structured JSON envelope instead of human output |  |

## `maina receipt`

Run the verify pipeline and emit a signed v1 receipt (JSON + HTML)

| Option | Description | Default |
|---|---|---|
| `--all` | verify all tracked files, not just the diff |  |
| `--base <branch>` | base branch for diff comparison | `master` |
| `--title <title>` | PR title (no auto-detection yet; defaults to 'Untitled PR') |  |
| `--output-dir <dir>` | override output directory (default .maina/receipts/) |  |
| `--no-index` | skip refreshing .maina/receipts/index.html after writing |  |
| `--json` | emit structured JSON envelope instead of human output |  |

### `maina receipt publish`

Post a receipt to its PR as one sticky comment and one check run (opt-in)

| Option | Description | Default |
|---|---|---|
| `--receipt <path>` | receipt JSON to publish |  |
| `--context <path>` | JSON with criteria, verify scope and gate tally |  |
| `--pr <number>` | pull request number |  |
| `--sha <sha>` | PR head commit for the check run |  |
| `--repo <owner/name>` | base repository (default $GITHUB_REPOSITORY) |  |
| `--scope-base <ref>` | the ref the verify run diffed against |  |
| `--receipt-url <url>` | link to the full receipt when --context gives none |  |
| `--opt-in` | the repository opted in to PR receipts |  |
| `--read-only` | the token cannot write (fork PR): use the job summary |  |
| `--no-discovery-line` | leave out the one-line Maina footer (the repo policy's discovery.receipt_line: false also does) |  |
| `--author <login>` | only update a sticky comment by this login |  |
| `--summary-file <path>` | fallback target (default $GITHUB_STEP_SUMMARY) |  |

## `maina apply-fix <hash> <check-id>`

Apply an autofix patch from a receipt as a follow-up commit

| Option | Description | Default |
|---|---|---|
| `--dry-run` | validate the patch without committing |  |
| `--base <branch>` | base branch for diff scope | `master` |
| `--json` | emit structured JSON envelope instead of human output |  |

## `maina commit`

Verify and commit staged changes

| Option | Description | Default |
|---|---|---|
| `-m, --message <msg>` | Commit message |  |
| `--skip` | Skip verification (not recommended) |  |
| `--no-verify` | Skip all verification and hooks |  |
| `--no-trailer` | Skip the Verified-by: Maina@sha256 commit trailer |  |
| `--json` | Output JSON for CI |  |

## `maina review`

Comprehensive code review (Superpowers-style)

| Option | Description | Default |
|---|---|---|
| `--base <ref>` | Base ref to diff against | `HEAD~1` |
| `--plan <path>` | Plan file for alignment check |  |
| `--json` | Output JSON for CI |  |

## `maina review-design <adr>`

Review an ADR against existing decisions and constitution

## `maina slop`

Check for AI-generated slop patterns

| Option | Description | Default |
|---|---|---|
| `--all` | Scan all tracked files, not just staged |  |
| `--json` | Output JSON |  |

## `maina pr`

Create a PR with two-stage review

| Option | Description | Default |
|---|---|---|
| `-t, --title <title>` | PR title |  |
| `--base <branch>` | Base branch | `main` |
| `--draft` | Create as draft PR |  |

## `maina run <task...>`

Run an agent on a task in maina's sandbox, gated, budgeted and reviewed

| Option | Description | Default |
|---|---|---|
| `--agent <name>` | Worker: claude, codex, cursor, gemini, opencode | `claude` |
| `--unattended` | Nobody will answer: every ask is denied |  |
| `--interactive` | Someone is at the terminal |  |
| `--pr` | Open a PR when the review passes |  |
| `--json` | Print the receipt as JSON |  |

## `maina acp`

Serve an agent to your editor over ACP, with maina's gate on its permission requests

| Option | Description | Default |
|---|---|---|
| `--agent <name>` | Worker: claude, codex, cursor, gemini, opencode | `claude` |

## `maina wiki`

Codebase knowledge wiki

### `maina wiki init`

Initialize wiki and run first compilation

| Option | Description | Default |
|---|---|---|
| `--ai` | Enhance articles with AI-generated descriptions |  |
| `--background` | Fork the compile into a background process and return immediately (G9) |  |
| `--depth <depth>` | Compile depth: 'quick' (sampled) or 'full' (default) | `full` |
| `--json` | Output JSON for CI |  |

### `maina wiki compile`

Compile wiki articles (incremental by default)

| Option | Description | Default |
|---|---|---|
| `--full` | Force full recompilation |  |
| `--dry-run` | Show what would change without writing |  |
| `--ai` | Enhance articles with AI-generated descriptions |  |
| `--json` | Output JSON for CI |  |

### `maina wiki query <question>`

Search wiki articles and synthesize an answer

| Option | Description | Default |
|---|---|---|
| `--save` | Save query results to wiki/raw/ |  |
| `--json` | Output JSON for CI |  |

### `maina wiki status`

Show wiki health dashboard

| Option | Description | Default |
|---|---|---|
| `--json` | Output JSON for CI |  |

### `maina wiki ingest <source>`

Ingest a local file into the wiki

| Option | Description | Default |
|---|---|---|
| `--json` | Output JSON for CI |  |

### `maina wiki lint`

Run wiki lint checks

| Option | Description | Default |
|---|---|---|
| `--json` | Output JSON for CI |  |

### `maina wiki export <format>`

Export the knowledge graph as cypher \| graphml \| obsidian (see #202)

| Option | Description | Default |
|---|---|---|
| `--out <path>` | Output path (defaults to ./maina-export.&lt;ext&gt; or ./maina-obsidian/) |  |
| `--json` | Output JSON for CI |  |

## `maina init`

Deprecated alias of `maina setup` (same flags)

| Option | Description | Default |
|---|---|---|
| `--update` | Refresh managed regions in agent files for the current stack |  |
| `--reset` | Back up .maina/ and start fresh |  |
| `--ci` | Non-interactive, JSON-per-phase output (sub-task 7) |  |
| `-y, --yes` | Skip prompts, accept defaults |  |
| `--agents <list>` | Comma-separated: agents,claude,cursor,copilot,windsurf |  |
| `--legacy-agents` | Also write retired agent files (.clinerules, .roo/, .aider.conf.yml, …) |  |
| `--plugin` | Non-interactive plugin mode: write only .maina/ and managed regions |  |
| `--no-telemetry` | Opt out of anonymous setup telemetry |  |
| `--json` | Machine-readable JSON output |  |
| `--force` | Ignored: setup never overwrites files |  |
| `--install` | Ignored: run `maina doctor` to see missing tools |  |

## `maina setup`

Onboard maina: detect → tailor → scaffold → verify (idempotent)

| Option | Description | Default |
|---|---|---|
| `--update` | Refresh managed regions in agent files for the current stack |  |
| `--reset` | Back up .maina/ and start fresh |  |
| `--ci` | Non-interactive, JSON-per-phase output (sub-task 7) |  |
| `-y, --yes` | Skip prompts, accept defaults |  |
| `--agents <list>` | Comma-separated: agents,claude,cursor,copilot,windsurf |  |
| `--legacy-agents` | Also write retired agent files (.clinerules, .roo/, .aider.conf.yml, …) |  |
| `--plugin` | Non-interactive plugin mode: write only .maina/ and managed regions |  |
| `--no-telemetry` | Opt out of anonymous setup telemetry |  |
| `--json` | Machine-readable JSON output |  |

## `maina doctor`

Check tool installation, engine health, and launch every configured MCP entry under its host's env

| Option | Description | Default |
|---|---|---|
| `--json` | Output JSON for CI |  |
| `--fix` | Run the `maina mcp add` fix for each missing MCP row and broken host entry |  |
| `-y, --yes` | Skip confirmations (with --fix) |  |
| `--launch-project` | Also launch project-scope MCP entries (e.g. .mcp.json) that are not maina's own launcher. Warning: this runs repo-controlled code; use it only in repos you trust |  |

## `maina login`

Log in to maina cloud via device authorization

| Option | Description | Default |
|---|---|---|
| `--github` | Log in with GitHub (device flow) |  |

## `maina logout`

Log out of maina cloud

## `maina configure`

(deprecated) alias for `maina setup --update` — removed in v1.5

| Option | Description | Default |
|---|---|---|
| `--no-interactive` | Run non-interactively (alias for `setup --update --ci`) |  |

## `maina mcp`

Install / remove / list the maina MCP server across AI clients

### `maina mcp add`

Install the maina MCP server in each detected client's global config

| Option | Description | Default |
|---|---|---|
| `--client <list>` | Comma-separated client IDs (default: auto-detect all) |  |
| `--scope <s>` | global \| project \| both (default: global) |  |
| `--dry-run` | Print what would change without writing |  |
| `--json` | Machine-readable JSON output |  |
| `-y, --yes` | Skip confirmations (no-op today; reserved) |  |

### `maina mcp remove`

Remove the maina MCP server from each client's config

| Option | Description | Default |
|---|---|---|
| `--client <list>` | Comma-separated client IDs (default: auto-detect all) |  |
| `--scope <s>` | global \| project \| both (default: global) |  |
| `--dry-run` | Print what would change without writing |  |
| `--json` | Machine-readable JSON output |  |
| `-y, --yes` | Skip confirmations (no-op today; reserved) |  |

### `maina mcp list`

Show maina MCP install status per client

| Option | Description | Default |
|---|---|---|
| `--client <list>` | Comma-separated client IDs (default: auto-detect all) |  |
| `--scope <s>` | global \| project \| both (default: global) |  |
| `--dry-run` | Print what would change without writing |  |
| `--json` | Machine-readable JSON output |  |
| `-y, --yes` | Skip confirmations (no-op today; reserved) |  |

## `maina privacy`

Show what maina may send off this machine, and why

| Option | Description | Default |
|---|---|---|
| `--json` | Print the effective collection config as JSON |  |

## `maina allow <decision-id>`

Override a gate decision from your terminal; --always also allows the action in your user policy

| Option | Description | Default |
|---|---|---|
| `--always` | Remember it: add an exact allow rule to ~/.maina/policy.json (never the repo policy) |  |
| `--json` | Print the result as JSON |  |

## `maina decide`

Answer one decision (e.g. action.risk) with your policy; --json for workflows

| Option | Description | Default |
|---|---|---|
| `--type <type>` | Decision type, such as action.risk |  |
| `--input <file>` | JSON request \{ state?, questions? \}; - reads stdin |  |
| `--trusted <key=value>` | A trusted state field (repeatable) |  |
| `--untrusted <key=value>` | An untrusted state field (repeatable) |  |
| `--json` | Print a \{ data, error, meta \} envelope |  |

## `maina digest`

Weekly gate digest from the decision log, with a card that is safe to share

| Option | Description | Default |
|---|---|---|
| `--week <yyyy-ww>` | ISO week to digest (default: this week) |  |
| `--send` | deliver the card to the channels in .maina/config.json `digest` (off unless configured) |  |
| `--card-labels` | also put tool names and deny rules on the card (they can name files or repos) |  |
| `--json` | emit a \{ data, error, meta \} envelope |  |

## Internal commands

Registered and callable by name, but left out of `maina --help`.

| Command | Description |
|---|---|
| `maina analyze` | Check spec ↔ plan ↔ tasks consistency |
| `maina benchmark` | Run benchmark comparing Spec Kit vs Maina |
| `maina cache` | Cache management commands |
| `maina context` | Generate focused codebase context |
| `maina explain` | Visualize codebase structure with Mermaid diagrams |
| `maina feedback` | Manage external review-finding ingestion (RL training signal) |
| `maina learn` | Analyse feedback and propose prompt improvements |
| `maina prompt` | Manage prompt templates and versions |
| `maina stats` | Show commit verification metrics and trends |
| `maina status` | Show current branch verification status |
| `maina sync` | Sync prompts with maina cloud |
| `maina team` | Manage your maina cloud team |
| `maina visual` | Visual verification management |
