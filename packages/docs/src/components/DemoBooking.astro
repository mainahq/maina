---
/**
 * DemoBooking — lightweight "book a demo" card. Scheduler choice is
 * open (plan.md Q2); until resolved, the CTA is a mailto. When a
 * scheduler lands the CTA gets rewired to a lazy-loaded embed.
 */
import { DEMO } from '../data/cloud-landing';

const subject = encodeURIComponent('Maina Cloud demo request');
const body = encodeURIComponent('Pick 2-3 time windows that work for you:\n- \n- \n- \n');
const href = `mailto:${DEMO.mailto}?subject=${subject}&body=${body}`;
---

<section class="demo" id="demo">
  <div class="demo__card">
    <span class="section-eyebrow">// demo</span>
    <h2 class="demo__title">{DEMO.header}</h2>
    <p class="demo__body">{DEMO.body}</p>
    <a class="demo__cta" href={href}>{DEMO.ctaLabel}</a>
  </div>
</section>

<style is:global>
  .demo {
    max-width: 720px;
    margin: 60px auto;
    padding: 0 24px;
  }

  .demo__card {
    background: var(--color-bg-elev);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: var(--radius);
    padding: 28px 28px 32px;
    text-align: center;
  }

  .demo__title {
    font-family: var(--font-display);
    font-size: clamp(22px, 2.4vw, 28px);
    margin: 8px 0 10px;
    color: var(--color-ink);
  }

  .demo__body {
    margin: 0 auto 20px;
    max-width: 540px;
    color: var(--color-ink-muted);
    font-size: 14.5px;
    line-height: 1.6;
  }

  .demo__cta {
    display: inline-block;
    padding: 11px 22px;
    background: var(--color-accent);
    color: var(--color-accent-ink);
    font-family: var(--font-mono);
    font-size: 13px;
    font-weight: 600;
    border-radius: var(--radius);
    text-decoration: none;
    transition: filter 140ms ease;
  }

  .demo__cta:hover { filter: brightness(1.06); }
</style>
